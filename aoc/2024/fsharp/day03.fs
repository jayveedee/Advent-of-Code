module day03

//let multiplyCorruptedData(input: int list list) : string ->
//    let pattern = @"^mul\((\d+),(\d+)\)$"

//    let regex = Regex(pattern)



let run (part: int) (input: string list list) : string =
    match part with
    | 1 -> ""
    | 2 -> ""
    | _ -> failwith "part Argument should only be 1 or 2"